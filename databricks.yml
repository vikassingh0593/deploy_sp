# Databricks Asset Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "data_modeling"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-2e4d9781-997b.cloud.databricks.com
      root_path: /Workspace/Users/vikassingh0593@gmail.com/git_deploy_dev


  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://dbc-2e4d9781-997b.cloud.databricks.com
      # We always use /Users/vikassingh0593@gmail.com for all resources to make sure we only have a single copy.
      root_path: /Users/vikassingh0593@gmail.com/.bundle/${bundle.name}/${bundle.target}
    run_as:
      # This runs as vikassingh0593@gmail.com in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: vikassingh0593@gmail.com

